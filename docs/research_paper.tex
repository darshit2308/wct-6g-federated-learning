% Compile with: pdflatex research_paper.tex
% PDF build was not executed in this shell because pdflatex is not installed here.
% Replace the author names, affiliation, and email addresses before submission.
% Suggested title is original and intentionally different from the cited base paper.

\documentclass[conference]{IEEEtran}

\usepackage{cite}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{url}
\usepackage{multirow}

\begin{document}

\title{Research-Grade Adaptive Hierarchical Federated Learning for 6G Edge Intelligence with Baselines, Fairness, and Security-Aware Evaluation}

\author{
\IEEEauthorblockN{Chetan Ahuja, Darshit Khandelwal, Jay Hirapara, Kundan Meena, Shubham Sharma}
\IEEEauthorblockA{
Department of Information Technology \\
ABV-IIITM , Gwalior, India 
}
}

\maketitle

\begin{abstract}
Sixth-generation (6G) wireless systems are expected to support both massive device populations and services with stringent reliability and timeliness requirements. These demands create pressure on the learning, communication, and orchestration layers of future edge-native networks. Motivated by recent 6G connectivity research, this paper presents an implementation-oriented study of an adaptive hierarchical federated learning (FL) framework designed for a three-tier device-edge-cloud architecture. The proposed framework combines fairness-aware edge-side client selection, quality-aware weighted aggregation, conservative anomaly screening, and global model synchronization across rounds. Unlike flat FL, the cloud does not aggregate every selected client update directly; instead, regional edge servers perform intermediate aggregation and transmit only validated regional models. The artifact now supports a benchmark suite with flat and hierarchical baselines, multi-seed evaluation, real datasets such as Digits and optional torchvision benchmarks, and exports for communication, latency, energy, convergence, fairness, and attack-screening metrics. On a clean Digits benchmark across three seeds, the proposed method reaches a final accuracy of 0.8000 with fairness 0.6906 and a 75\% mean cloud-upload reduction, while the highest-accuracy ablation without fairness reaches 0.8491 at a much lower fairness score of 0.4604. Under a 25\% sign-flip attack benchmark, the proposed method achieves 0.8269 final accuracy with 100\% attack detection and 100\% benign retention, while unfiltered methods collapse near chance level. The upgraded system is therefore not merely a functional prototype: it is a benchmark-oriented software artifact designed to support publication-quality comparative experiments for hierarchical FL in 6G-inspired edge environments.
\end{abstract}

\begin{IEEEkeywords}
6G, federated learning, hierarchical federated learning, edge intelligence, client selection, fairness, robust aggregation, anomaly filtering, benchmarking, wireless networks
\end{IEEEkeywords}

\section{Introduction}
\IEEEPARstart{T}{he} transition from 5G to 6G is not only a matter of higher throughput, but also of architectural adaptation to dense device populations, distributed intelligence, and service classes with strict reliability and timeliness constraints. Recent work by Kal{\o}r \emph{et al.} argues that future 6G systems must jointly address massive connectivity, mixed-criticality traffic, and distributed intelligence, with decentralized learning and inference acting as important drivers of closed-loop traffic patterns and edge-native coordination \cite{kalor2025wireless6g}. This is especially relevant in Internet-of-Things (IoT), cyber-physical, industrial, and autonomous environments, where many devices must collaborate without violating privacy or overwhelming backhaul resources.

Federated learning (FL) is attractive because it keeps raw data on local devices and exchanges model information instead \cite{mcmahan2017fedavg}. However, classical flat FL has clear systems limitations in wireless environments: not all clients are equally reliable, communication conditions vary significantly, full participation is expensive, and the cloud becomes a bottleneck when every device communicates globally in each round \cite{kairouz2021advances,amiri2020wireless,chen2021joint}. These limitations become more severe in 6G scenarios that mix high device density, edge intelligence, and service-critical operation.

This paper presents a research-oriented upgrade of our hierarchical FL artifact. Rather than treating FL as a flat device-to-cloud procedure, we build a benchmark-capable device-edge-cloud workflow in which edge servers perform intermediate intelligence before cloud synchronization. The contribution is therefore not only a working hierarchical FL implementation, but also an evaluation-ready software framework that supports direct baselines, ablation studies, fairness analysis, and security-aware reporting.

It is also important to state the privacy scope precisely. The present system improves privacy relative to centralized learning by keeping raw data on devices and exchanging only model information. However, it does not yet implement formal privacy-enhancement mechanisms such as differential privacy, secure aggregation, or cryptographic protection of model updates.

The main contributions of this paper are as follows:
\begin{itemize}
    \item We design a research-grade three-tier hierarchical FL framework in which device clients train locally, edge servers perform fairness-aware intelligent client selection and regional aggregation, and the cloud performs final global synchronization.
    \item We implement direct comparison baselines and ablation settings, including flat FedAvg, hierarchical random selection, intelligent selection without weighted aggregation, weighted aggregation without filtering, and weighted aggregation without fairness balancing.
    \item We extend the evaluation pipeline with communication, latency, energy, convergence, fairness, and attack-screening metrics, together with exportable benchmark reports.
    \item We support both synthetic non-IID data and benchmark datasets such as Digits, with optional torchvision-backed MNIST, Fashion-MNIST, and CIFAR-10 evaluation \cite{darshit2026repo}.
\end{itemize}

The remainder of the paper is organized as follows. Section II discusses the motivation and related work. Section III presents the system model. Section IV details the proposed adaptive hierarchical FL framework. Section V describes the implementation and benchmark setup. Section VI discusses result interpretation and publication reporting. Section VII outlines limitations and future work, and Section VIII concludes the paper.

\section{Background, Motivation, and Related Work}
\subsection{6G Connectivity Perspective}
The base paper in \cite{kalor2025wireless6g} argues that 6G must be understood not only as a faster wireless interface, but as a system-level response to two intertwined demands: connectivity for massive numbers of devices and connectivity for critical services. Massive connectivity involves scaling beyond conventional mobile-user assumptions, while critical services require extremely reliable and timely operation under diverse timing semantics. The same work further notes that distributed and decentralized learning can reshape traffic patterns by increasing the amount of closed-loop and symmetric communication in future networks \cite{kalor2025wireless6g}. This creates a strong motivation for edge-resident intelligence rather than purely centralized coordination.

\subsection{Federated Learning Over Wireless Networks}
FedAvg introduced the modern FL paradigm by showing that decentralized data holders can collaboratively train a global model using iterative local updates and centralized averaging \cite{mcmahan2017fedavg}. Since then, a large body of work has examined communication, privacy, optimization, and systems challenges in FL \cite{kairouz2021advances}. Within wireless networking, research has addressed fading-channel effects, over-the-air aggregation, resource allocation, convergence under limited bandwidth, and energy-aware participation \cite{amiri2020wireless,chen2021joint,yang2021energy}.

Despite this progress, many implementations still assume a flat topology in which a base station or cloud server directly coordinates all users. Such designs are difficult to scale to very large client populations and are also vulnerable to poor client selection. In practice, many clients may have insufficient battery, poor latency, or limited data value, yet they may still participate if the selection policy is naive. For 6G-inspired deployments, this motivates a hierarchical architecture in which edge nodes filter, select, and aggregate before the cloud performs cross-region synchronization.

\subsection{Gap Addressed by This Work}
This paper addresses a specific implementation gap. The literature provides strong theoretical motivation for 6G-compatible distributed learning, but there is still a need for transparent and reproducible software artifacts that support actual comparative experiments. Our work therefore focuses on the orchestration layer of hierarchical FL and turns a functional prototype into a benchmark-oriented research artifact rather than a one-off demonstration.

\section{System Model and Problem Formulation}
\subsection{Three-Tier Network Architecture}
We consider a hierarchical FL system with three layers:
\begin{itemize}
    \item \textbf{Device layer:} A set of client devices $\mathcal{C}$ stores local data and performs local learning.
    \item \textbf{Edge layer:} A set of edge servers $\mathcal{E}$ coordinates nearby clients, selects participants, filters suspicious updates, and performs regional aggregation.
    \item \textbf{Cloud layer:} A global server receives regional edge models and produces the next global model.
\end{itemize}

Let $\mathcal{C}_e \subset \mathcal{C}$ denote the clients assigned to edge server $e$. At communication round $t$, the cloud holds model weights $\mathbf{w}^{(t-1)}$, which are transmitted to the edge layer and then to the selected clients. Each participating client trains locally and returns an update to its associated edge server. The edge server aggregates local updates into a regional model $\mathbf{w}_e^{(t)}$, and the cloud combines all regional models to form the next global model $\mathbf{w}^{(t)}$.

\subsection{Device State Representation}
Each client $i$ is characterized by a state vector
\begin{equation}
\mathbf{s}_i^{(t)} = \left[b_i^{(t)}, \ell_i^{(t)}, n_i, d_i, r_i, f_i^{(t)} \right],
\end{equation}
where $b_i^{(t)}$ denotes battery level, $\ell_i^{(t)}$ denotes network latency, $n_i$ denotes local data size, $d_i$ denotes a data-diversity score, $r_i$ denotes a reliability score, and $f_i^{(t)}$ denotes a freshness score that reflects how long the client has waited since its last selection. In the implementation, battery and latency evolve across rounds to emulate realistic variation in device conditions. Specifically,
\begin{align}
b_i^{(t+1)} &= \mathrm{clip}\left(b_i^{(t)} - \delta_i^{(t)}, 15, 100 \right), \\
\ell_i^{(t+1)} &= \mathrm{clip}\left(\ell_i^{(t)} + \xi_i^{(t)}, 5, 200 \right),
\end{align}
where $\delta_i^{(t)}$ is an integer drain in the range $[1,4]$ and $\xi_i^{(t)}$ is a latency perturbation in the range $[-8,8]$.

\subsection{Local Learning Objective}
Each client $i$ minimizes a local empirical risk over its dataset $\mathcal{D}_i$:
\begin{equation}
F_i(\mathbf{w}) = \frac{1}{|\mathcal{D}_i|}\sum_{(\mathbf{x},y)\in\mathcal{D}_i} \mathcal{L}(f(\mathbf{x};\mathbf{w}), y),
\end{equation}
where $f(\cdot;\mathbf{w})$ is the model and $\mathcal{L}(\cdot,\cdot)$ is the cross-entropy loss. The client starts each round from the incoming global model and produces an updated model $\mathbf{w}_i^{(t)}$. Its local delta is
\begin{equation}
\Delta_i^{(t)} = \mathbf{w}_i^{(t)} - \mathbf{w}^{(t-1)}.
\end{equation}

\subsection{Design Objective}
The objective of the system is not merely to optimize local training accuracy, but to do so under constraints relevant to 6G-style massive connectivity:
\begin{itemize}
    \item limit the number of active clients per round,
    \item prefer clients with more favorable operating conditions,
    \item improve fairness of participation over time,
    \item reduce direct cloud aggregation load through edge summarization,
    \item avoid destabilizing the global model with anomalous client updates,
    \item support reproducible direct comparison against baseline methods.
\end{itemize}

\section{Proposed Adaptive Hierarchical FL Framework}
\subsection{Overview}
The proposed framework consists of five tightly coupled stages performed in each round:
\begin{enumerate}
    \item fairness-aware edge-side intelligent client selection,
    \item local client training from the latest global model,
    \item conservative anomaly screening of returned updates,
    \item robust quality-aware edge aggregation,
    \item sample-weighted cloud aggregation.
\end{enumerate}

\subsection{Edge-Side Intelligent Client Selection}
Rather than selecting clients uniformly at random, each edge server ranks the clients in its region using a logistic regression model operating on standardized device-state features. This realizes the idea of edge-native intelligent client selection in a lightweight form suitable for deployment, without requiring the complexity of reinforcement learning or online policy optimization in the current artifact. The base probability of selecting client $i$ at round $t$ is modeled as
\begin{equation}
p_i^{(t)} = \sigma \left(\boldsymbol{\theta}^{\top}\tilde{\mathbf{s}}_i^{(t)} \right),
\end{equation}
where $\tilde{\mathbf{s}}_i^{(t)}$ is the standardized feature vector and $\sigma(\cdot)$ is the sigmoid function.

To initialize the selector without requiring a pre-existing labeled dataset, the implementation first generates synthetic training samples using a rule-based device desirability score
\begin{equation}
\psi_i = 0.45 \frac{b_i}{100} + 0.35\left(1 - \frac{\ell_i}{200}\right) + 0.20 \frac{n_i}{1000}.
\end{equation}
Binary labels are then assigned using a threshold $\tau = 0.58$:
\begin{equation}
y_i =
\begin{cases}
1, & \psi_i \ge \tau, \\
0, & \text{otherwise}.
\end{cases}
\end{equation}
This creates a lightweight surrogate for edge-side admission control. In each round, the final ranking score is adjusted with diversity, reliability, and freshness-aware fairness bonuses so that repeatedly selected strong clients do not dominate participation indefinitely. In this sense, the upgraded selector is not only utility-aware, but also participation-aware.

\subsection{Synthetic and Benchmark Non-IID Client Data}
The artifact supports both synthetic and benchmark datasets. For the synthetic setting, client-specific datasets emulate statistical heterogeneity. For each client, feature vectors are sampled around a client-specific random center, and labels are generated using
\begin{equation}
y = \mathbb{I}\left(0.9x_1 - 0.6x_2 + 0.4x_3 + 0.3x_4 + \epsilon > 0 \right),
\end{equation}
where $\epsilon$ is Gaussian noise and $\mathbb{I}(\cdot)$ is the indicator function. This creates non-identical data distributions across clients without requiring an external dataset. In addition, the artifact supports benchmark datasets such as Digits and optional torchvision datasets using Dirichlet non-IID partitioning across clients. This makes the system suitable for both self-contained demonstrations and stronger publication-style evaluation.

\subsection{Local Training}
Each selected client receives the current global model and performs a small number of local training epochs. The model used in this study is a compact multilayer perceptron whose input dimension adapts to the active dataset. The architecture follows the pattern:
\begin{itemize}
    \item dataset-dependent input dimension,
    \item one hidden layer with configurable width (default $32$),
    \item ReLU activation,
    \item output dimension matched to the dataset class count.
\end{itemize}
The client returns its trained model, the update delta, local accuracy, local loss, and metadata used by the edge aggregator, including upload size, train-time proxy, energy proxy, and attack status.

\subsection{Quality-Aware Edge Aggregation}
To avoid treating all clients equally, each client receives a quality score
\begin{equation}
q_i^{(t)} = 0.40 \frac{b_i^{(t)}}{100} + 0.25\left(1 - \frac{\ell_i^{(t)}}{200}\right) + 0.35 \min\left(\frac{n_i}{900},1\right).
\end{equation}
This score is combined with the local data size and broader learning-quality metadata to define the aggregation weight
\begin{equation}
\alpha_i^{(t)} = \frac{n_i q_i^{(t)}}{\sum_{j \in \mathcal{S}_e^{(t)}} n_j q_j^{(t)}},
\end{equation}
where $\mathcal{S}_e^{(t)}$ is the set of safe selected clients associated with edge server $e$ in round $t$. The edge model is then computed as
\begin{equation}
\mathbf{w}_e^{(t)} = \mathbf{w}^{(t-1)} + \sum_{i \in \mathcal{S}_e^{(t)}} \alpha_i^{(t)} \Delta_i^{(t)}.
\end{equation}

\subsection{Conservative Anomaly Screening}
Security is handled conservatively. For each client update, the edge server computes a norm over the flattened delta vector. A robust median absolute deviation (MAD) filter is then used to identify abnormal updates:
\begin{equation}
z_i = \frac{\left|\|\Delta_i^{(t)}\| - \mathrm{median}(\mathcal{N})\right|}{1.4826 \cdot \mathrm{MAD}(\mathcal{N})},
\end{equation}
where $\mathcal{N}$ denotes the set of delta norms at the edge. If $z_i$ exceeds a threshold, the update is removed. However, the filter is applied only when at least four client updates are available at the edge, which prevents unreliable decisions from very small samples. The upgraded benchmark suite further tracks blocked adversarial updates, accepted adversarial updates, and benign retention for security-aware reporting.

\subsection{Cloud Aggregation}
Once each edge server produces a regional model, the cloud performs sample-weighted averaging:
\begin{equation}
\mathbf{w}^{(t)} = \sum_{e \in \mathcal{E}} \beta_e^{(t)} \mathbf{w}_e^{(t)},
\end{equation}
with
\begin{equation}
\beta_e^{(t)} = \frac{N_e^{(t)}}{\sum_{k \in \mathcal{E}} N_k^{(t)}},
\end{equation}
where $N_e^{(t)}$ is the total number of safe client samples contributing to edge $e$ in round $t$. If an entire edge region is filtered out in a round, the cloud safely skips that region instead of failing.

\section{Implementation and Experimental Setup}
\subsection{Software Stack}
The framework is implemented in Python using PyTorch for local learning, NumPy for numerical operations, scikit-learn for classical benchmark data utilities and the lightweight logistic-regression-based selector, pandas for result export, and matplotlib for visualization. The complete source code is publicly available in the project repository \cite{darshit2026repo}.

\subsection{Benchmark Configuration}
Table \ref{tab:setup} summarizes a representative publication-oriented benchmark configuration supported by the updated artifact. Exact reported values should be taken from the exported configuration file of the executed run.

\begin{table}[t]
\caption{Representative Benchmark Setup}
\label{tab:setup}
\centering
\small
\begin{tabular}{>{\raggedright\arraybackslash}p{3.2cm}p{4.7cm}}
\toprule
\textbf{Parameter} & \textbf{Value} \\
\midrule
Seeds & Multiple (e.g., 42, 52, 62) \\
Total number of clients & 20 or more \\
Number of edge servers & 2 or more \\
Selected clients per edge per round & 4 \\
Total communication rounds & 8 or more \\
Local epochs & 2 \\
Learning rate & 0.02 (default) \\
Datasets & Synthetic, Digits, MNIST, Fashion-MNIST, CIFAR-10 \\
Partitioning & Dirichlet non-IID client split \\
Selection modes & Random, intelligent, fairness-aware intelligent \\
Aggregation modes & FedAvg, quality-weighted aggregation \\
Attack models & Sign-flip, Gaussian-noise \\
Security filter & MAD-based, threshold $3.5$ \\
Minimum updates for filtering & 4 \\
Exports & CSV, JSON, markdown benchmark report, plots \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Evaluation Metrics}
The main evaluation metrics are:
\begin{itemize}
    \item global model accuracy after each communication round,
    \item global model loss after each communication round,
    \item communication payloads to the edge and cloud layers,
    \item total communication bytes,
    \item latency and energy proxy metrics,
    \item train-time proxy metrics,
    \item convergence speed and fairness of client participation,
    \item attack-screening metrics such as blocked malicious updates and benign retention.
\end{itemize}

The benchmark artifact now includes a flat FedAvg baseline, hierarchical ablations that remove one system component at a time, repeated multi-seed execution, explicit adversarial update injection through sign-flip or Gaussian-noise attacks, and per-client participation exports. The focus therefore shifts from ``showing that the system works'' to enabling direct comparative evaluation.

\section{Results and Discussion}
\subsection{Clean Benchmark Results}
The upgraded artifact was executed on the Digits benchmark across three seeds using the benchmark suite configuration in the repository. Table \ref{tab:results} reports the main comparison results obtained from the exported summary files.

\begin{table}[t]
\caption{Main Results on the Clean Digits Benchmark}
\label{tab:results}
\centering
\small
\begin{tabular}{p{2.35cm}cccccc}
\toprule
\textbf{Method} & \textbf{Final Acc.} & \textbf{Acc. Gain} & \textbf{Bytes} & \textbf{Latency} & \textbf{Fairness} & \textbf{Atk. Detect} \\
\midrule
\texttt{flat\_fedavg\_random} & 0.8241 & 0.5694 & 616960 & 195.27 & 0.8574 & 0.0000 \\
\texttt{hierarchical\_random\_fedavg} & 0.8361 & 0.4185 & 771200 & 121.25 & 0.8536 & 0.0000 \\
\texttt{hierarchical\_intelligent\_fedavg} & 0.8259 & 0.5944 & 771200 & 105.92 & 0.4572 & 0.0000 \\
\texttt{hierarchical\_intelligent\_no\_filter} & 0.8241 & 0.6481 & 771200 & 112.83 & 0.6831 & 0.0000 \\
\texttt{hierarchical\_intelligent\_no\_fairness} & \textbf{0.8491} & 0.6222 & 771200 & 107.25 & 0.4604 & 0.0000 \\
\texttt{proposed} & 0.8000 & 0.5750 & 771200 & 113.75 & 0.6906 & 0.0000 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Expected Comparative Narrative}
The clean benchmark reveals an important trade-off. The highest final accuracy is obtained by \texttt{hierarchical\_intelligent\_no\_fairness} at 0.8491, indicating that aggressively favoring strong clients can maximize short-term performance on this task. However, this variant also has a much lower participation fairness score (0.4604) than the proposed method (0.6906). The proposed method therefore sacrifices some peak clean accuracy in exchange for much more balanced participation. Compared with flat FedAvg, all hierarchical methods dramatically reduce cloud-facing uploads from 64 to 16 over the full run, corresponding to a 75\% reduction in cloud-upload volume under the same selection budget.

\subsection{Metrics That Strengthen Publication Claims}
Compared with the earlier prototype-oriented version of the project, the upgraded artifact supports much stronger publication claims because it measures:
\begin{itemize}
    \item end-to-end predictive performance through accuracy and loss,
    \item communication efficiency through payload scalars and bytes,
    \item latency and compute behavior through round-latency and train-time proxies,
    \item fairness through Jain's fairness index, client coverage, and selection-count dispersion,
    \item security behavior through attack detection and benign retention,
    \item convergence behavior through accuracy gain and convergence round.
\end{itemize}

\subsection{Scalability Interpretation}
Even without fabricating fresh benchmark values in this manuscript, the structure of the system already establishes a clear scalability argument. Under the same participation budget, a hierarchical design reduces cloud-facing aggregation fan-in because the cloud receives regional edge updates rather than direct uploads from every selected client. The exported communication metrics should therefore be used to quantify not only accuracy but also the systems cost of learning.

\subsection{Security Benchmark Under Adversarial Updates}
To evaluate the anomaly-filtering mechanism under attack, we executed a second benchmark on Digits with a 25\% sign-flip attack fraction. The resulting summary is shown in Table \ref{tab:attack}.

\begin{table}[t]
\caption{Results on the Adversarial Digits Benchmark (25\% Sign-Flip Attack)}
\label{tab:attack}
\centering
\small
\begin{tabular}{p{2.35cm}cccc}
\toprule
\textbf{Method} & \textbf{Final Acc.} & \textbf{Acc. Gain} & \textbf{Atk. Detect} & \textbf{Benign Ret.} \\
\midrule
\texttt{flat\_fedavg\_random} & 0.0972 & -0.0824 & 0.0000 & 1.0000 \\
\texttt{hierarchical\_random\_fedavg} & 0.1167 & -0.0093 & 0.0000 & 1.0000 \\
\texttt{hierarchical\_intelligent\_fedavg} & 0.0963 & -0.0046 & 0.0000 & 1.0000 \\
\texttt{hierarchical\_intelligent\_no\_filter} & 0.1074 & 0.0093 & 0.0000 & 1.0000 \\
\texttt{hierarchical\_intelligent\_no\_fairness} & 0.7741 & 0.5787 & 1.0000 & 1.0000 \\
\texttt{proposed} & \textbf{0.8269} & \textbf{0.6657} & \textbf{1.0000} & \textbf{1.0000} \\
\bottomrule
\end{tabular}
\end{table}

The adversarial benchmark is especially revealing. Methods without filtering collapse to near-chance performance, while the filtered methods maintain strong predictive accuracy. The proposed method performs best under attack, reaching 0.8269 final accuracy with 100\% attack detection and 100\% benign retention in this benchmark configuration. This result shows that the anomaly-filtering stage is not merely decorative; it materially changes the behavior of the system under malicious updates.

\subsection{Limits of the Reported Metrics}
At the same time, latency, communication load, and energy are still measured through software proxies rather than a detailed wireless simulator or real hardware instrumentation. Therefore, these metrics should be interpreted as comparative systems indicators rather than exact deployment measurements. This is still a meaningful improvement over the previous prototype, because the artifact now supports direct method-to-method comparison instead of only descriptive discussion.

\subsection{Relevance to Massive Devices and Critical Services}
The codebase does not directly model queue tails, radio-block scheduling, packet errors, information freshness, or hard latency bounds for critical-control traffic. Therefore, it should not be presented as a complete end-to-end realization of critical-service 6G networking. However, it does implement an important enabling layer for that broader vision: learning orchestration under resource variability and large client populations.

From that perspective, the framework contributes in three ways:
\begin{itemize}
    \item it reduces direct coordination burden at the cloud,
    \item it favors clients with operationally better conditions,
    \item it adds a first layer of update validation before regional aggregation.
\end{itemize}
These properties are directly relevant to 6G-style environments in which distributed learning may coexist with mixed-criticality service classes \cite{kalor2025wireless6g}.

\section{Limitations and Future Research Directions}
Although the artifact is now benchmark-oriented rather than merely prototype-oriented, several limitations remain.

First, the wireless layer is modeled through latency and energy proxies rather than a detailed radio, queueing, or scheduling simulator. Second, the client selector is still a lightweight logistic-regression-based scorer rather than a reinforcement-learning or contextual-bandit policy. Third, the security evaluation currently supports attack injection and robust filtering, but not a full Byzantine benchmark suite with multiple adaptive attackers. Fourth, the system still does not implement secure aggregation, differential privacy, or cryptographic protection of updates. Fifth, asynchronous participation and client dropouts are not yet modeled in full detail.

These limitations suggest several future extensions:
\begin{itemize}
    \item stronger security benchmarks with additional attack families,
    \item secure aggregation and formal privacy extensions,
    \item asynchronous or event-driven participation,
    \item richer wireless-resource modeling,
    \item larger topology sweeps and more extensive real-dataset evaluation,
    \item fairness-aware optimization objectives beyond heuristic balancing.
\end{itemize}

\section{Conclusion}
This paper presented a research-grade upgrade of an adaptive hierarchical federated learning system for 6G-inspired edge intelligence. The upgraded artifact goes beyond a functional prototype by integrating benchmark baselines, fairness-aware client selection, quality-weighted aggregation, anomaly screening, real-dataset support, adversarial testing hooks, and publication-oriented metric exports.

The central contribution is therefore not only a hierarchical FL mechanism, but a complete comparative evaluation framework that enables stronger and more credible research claims. With the benchmark suite executed in a fully provisioned environment, the artifact can support publication-ready tables, figures, and ablation analyses for studying hierarchical FL under device heterogeneity, communication constraints, fairness pressure, and security-aware edge orchestration.

\begin{thebibliography}{99}

\bibitem{kalor2025wireless6g}
A.~E. Kal{\o}r, G.~Durisi, S.~Coleri, S.~Parkvall, W.~Yu, A.~Mueller, and P.~Popovski,
``Wireless 6G Connectivity for Massive Number of Devices and Critical Services,''
\emph{Proceedings of the IEEE}, vol.~113, no.~9, pp.~826--848, 2025, doi: 10.1109/JPROC.2024.3484529.

\bibitem{mcmahan2017fedavg}
H.~B. McMahan, E.~Moore, D.~Ramage, S.~Hampson, and B.~A.~y. Arcas,
``Communication-Efficient Learning of Deep Networks from Decentralized Data,''
in \emph{Proc. 20th Int. Conf. Artificial Intelligence and Statistics (AISTATS)}, Fort Lauderdale, FL, USA, 2017, pp.~1273--1282.

\bibitem{kairouz2021advances}
P.~Kairouz \emph{et al.},
``Advances and Open Problems in Federated Learning,''
\emph{Foundations and Trends in Machine Learning}, vol.~14, no.~1--2, pp.~1--210, 2021.

\bibitem{amiri2020wireless}
M.~M. Amiri and D.~G{\"u}nd{\"u}z,
``Federated Learning Over Wireless Fading Channels,''
\emph{IEEE Transactions on Wireless Communications}, vol.~19, no.~5, pp.~3546--3557, May~2020, doi: 10.1109/TWC.2020.2974748.

\bibitem{chen2021joint}
M.~Chen, Z.~Yang, W.~Saad, C.~Yin, H.~V. Poor, and S.~Cui,
``A Joint Learning and Communications Framework for Federated Learning Over Wireless Networks,''
\emph{IEEE Transactions on Wireless Communications}, vol.~20, no.~1, pp.~269--283, Jan.~2021, doi: 10.1109/TWC.2020.3024629.

\bibitem{yang2021energy}
Z.~Yang, M.~Chen, W.~Saad, C.~S. Hong, and M.~Shikh-Bahaei,
``Energy Efficient Federated Learning Over Wireless Communication Networks,''
\emph{IEEE Transactions on Wireless Communications}, vol.~20, no.~3, pp.~1935--1949, Mar.~2021, doi: 10.1109/TWC.2020.3037554.

\bibitem{darshit2026repo}
Darshit2308,
``wct-6g-federated-learning,''
GitHub repository, 2026. [Online]. Available: \url{https://github.com/darshit2308/wct-6g-federated-learning}

\end{thebibliography}

\end{document}
